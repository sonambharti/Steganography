#   Backend Setup
Commands to setup backend:
```
cd backend
npm init -y
npm install express multer jimp cors
npm install --save-dev typescript ts-node @types/express @types/node @types/multer
npx tsc --init
```
